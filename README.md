# Secure Password Manager (Android)

A simple Android password manager built with .NET (Xamarin), focused on local security and clean architecture.  
The app allows users to register, log in, and securely store encrypted passwords per account using SQLite.

Built as a learning project with emphasis on separation of concerns, basic cryptography, and modern UI design.
